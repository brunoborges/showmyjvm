kind: Service
apiVersion: v1
metadata:
  name: helidon-mp
  labels:
    app: helidon-mp
spec:
  type: ClusterIP
  selector:
    app: helidon-mp
  ports:
    - name: tcp
      port: 8080
      protocol: TCP
      targetPort: 8080
---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: helidon-mp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: helidon-mp
  template:
    metadata:
      labels:
        app: helidon-mp
        version: v1
    spec:
      containers:
      - name: helidon-mp
        image: helidon-mp
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
